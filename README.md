# Jeu-du-Pendu : trouver un mot en proposant des lettres avant de finir la potence

Utilisation : -Télécharger les fichiers
              -Créer un projet dans Eclipse JEE
              -Ajouter les fichiers java et celui en jsp(dans le webapp)
              -Mettre le xml et le txt dans un dossier (
              -Modifier les adresses dans Lecteur.java(fichier .txt) et dans ScorePendu.java(fichier .xml)
              -lancer le serveur dans Eclipse ou Faire un Maven Install pour obtenir un .war
